<view class="container">
  <view class="page-body">
    <view style="margin-bottom: 120rpx">
      <product bindsubmit="onSubmitOrder" id="product"/>
    </view>
    <view wx:if="{{product.price}}" class="buy-button" bindtap="onPay">￥{{product.price/100}} 立即购买</view>
  </view>
</view>
