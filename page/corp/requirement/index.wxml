<import src="../../common/empty.wxml" />
<block wx:if="{{isEmpty}}">
  <template is="empty" data="{{content: '暂时还没有需求上架～'}}"/>
</block>
<view wx:else class="container">
  <view class="page-body">
    <view wx:for="{{requirement}}" wx:for-index="index" wx:key="item" class="requirement-item-wrap">
      <exhibit-item url="shelve/index?id={{item._id}}"
        candelete="true"
        data-index="{{index}}"
        bind:delete="deleteRequirement"
        imgpath="{{item.cover}}"
        title="{{item.name}}"
        body="{{item.brief}}"
        footer=""
      />
    </view>
  </view>
</view>
<view class="float-bar">
  <view bindtap="shelve" class="bar-item">需求上架</view>
</view>