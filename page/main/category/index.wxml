<view wx:for="{{products}}" wx:key="item" class="exhibit-item-wrapper">
  <exhibit-item url="../../corp-product/product-detail/index?id={{item._id}}"
    imgpath="{{item.cover}}"
    title="{{item.name}}"
    body="{{item.brief}}"
    footer=""
  />
</view>